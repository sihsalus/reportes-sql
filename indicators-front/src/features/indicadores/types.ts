/**
 * Domain types for the indicadores feature.
 *
 * Re-exports shared API types so feature consumers import from one place.
 */

export type { Indicador, PaginatedResponse, ApiError } from '@/api/types';
